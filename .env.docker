# ---------- Database (container-to-container; host is 'db') ----------
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/task_api

# ---------- Storage ----------
# Switch to 's3' if you want to test presigned uploads from inside the container
STORAGE_BACKEND=s3

AWS_REGION=ap-south-1
AWS_S3_BUCKET=taskapi-medhini-dev
AWS_S3_PREFIX=attachments/

# Only used if STORAGE_BACKEND=local
LOCAL_STORAGE_DIR=./uploaded_files

# Presigned URL expiry (seconds)
PRESIGNED_EXPIRES_SECONDS=900

# Optional: inject creds here for S3 testing from container (or pass with `docker compose run -e ...`)
# AWS_ACCESS_KEY_ID=YOUR_KEY_ID
# AWS_SECRET_ACCESS_KEY=YOUR_SECRET